requests>=2.31
duckdb>=1.0
polars>=1.0
pyarrow>=15.0
python-dateutil>=2.9
tenacity>=8.2

# modeling (added as needed)
scikit-learn>=1.4
numpy>=1.26

# CS2 demo parsing (round/tick-level data) — added when we build that layer
# awpy>=2.0
